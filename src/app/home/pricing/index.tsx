import React from "react";

interface packageProps {
   buttonName: string;
   amount: string;
}

function packageDetails(props: packageProps) {
   const {buttonName, amount} = props;

   return <div></div>;
}

export default packageDetails;
